class Standard {
  String stdValue = '';
  String stdText = '';
  bool isSelected = false;

  Standard(
      {required this.stdValue,
      required this.stdText,
      required this.isSelected});
}
